name: First workflow 
on: workflow_dispatch 
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run:  | 
              echo "Hello world"
              echo "welcome"
      - name: Print good bye
        run: echo "Flutter developer job made easy"
    
